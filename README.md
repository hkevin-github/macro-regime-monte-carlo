# macro-regime-monte-carlo
An institutional-grade portfolio risk engine that resolves the non-stationarity flaw in standard Monte Carlo simulations. Built with Python, it pairs an unsupervised Gaussian Hidden Markov Model (HMM) with state-dependent block bootstrapping to dynamically infer macro regimes (FRED/YF) and project regime-conditioned, asset-correlated return paths.
